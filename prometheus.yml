global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'techstream_flask'
    ec2_sd_configs:
      - region: us-east-1
        port: 8000
        filters:
          - name: tag:Role
            values: [TechStream-App]
    metrics_path: /metrics
